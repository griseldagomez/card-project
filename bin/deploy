#! /usr/bin/bash

APP_ENV=prod && symfony composer install --no-dev

npm install --no-save

npm run build

APP_ENV=prod && symfony console asset-map:compile

APP_ENV=prod && symfony console cache:warmup
