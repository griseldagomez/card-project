framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
            - vendor/shadowc/adminbundle/assets/dist/
        excluded_patterns:
            - '*/assets/scss/*.scss'
            - '*/assets/typescript-controllers/*.ts'
            - '*/assets/react/*.tsx'
